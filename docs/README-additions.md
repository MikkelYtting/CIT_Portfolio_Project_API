Bilag og eksempler (hurtig reference)

Filer oprettet i `docs/`:

- `docs/examples/page-movie-example.json` — JSON-eksempel for PageDto<MovieDto> (pagineret movies-respons).
- `docs/architecture.svg` — simpelt høj-niveau UML-diagram (SVG) der viser Controllers → Managers → Repositories → DbContext + DTO/Entity relation.

Brug:
- Inkluder `page-movie-example.json` i din afleveringsrapport som et eksempel på "outer-facing contract".
- Åbn `architecture.svg` og indsæt som figur i afleveringen (konverter til PNG hvis nødvendigt).

Vil du have flere eksempler (Bookmarks, Ratings) eller en PNG-version af diagrammet, siger du bare til — så laver jeg dem også.
